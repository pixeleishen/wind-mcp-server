# LLM 配置（复制为 .env 并填写）

# ── Provider ────────────────────────────────────────────────
# 可选值：openai | anthropic | deepseek | ollama
LLM_PROVIDER=openai

# ── API Key ─────────────────────────────────────────────────
LLM_API_KEY=sk-xxxx

# ── 模型名 ──────────────────────────────────────────────────
# OpenAI:    gpt-4o / gpt-4o-mini / gpt-4-turbo
# Anthropic: claude-opus-4-5 / claude-sonnet-4-5
# DeepSeek:  deepseek-chat / deepseek-reasoner
# Ollama:    qwen2.5-coder:32b / llama3.3 / ...
LLM_MODEL=gpt-4o

# ── Base URL（可选）─────────────────────────────────────────
# 留空则使用各 provider 默认地址
# Ollama 本地示例：http://localhost:11434
# OpenAI 代理示例：https://your-proxy.com/v1
LLM_BASE_URL=
